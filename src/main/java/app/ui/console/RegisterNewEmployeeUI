package app.ui.console;

import app.controller.RegisterNewEmployeeController;
import app.controller.App;
import app.domain.model.Company;
import app.domain.model.Employee;
import app.domain.shared.Constants;
import app.ui.console.utils.Utils;


import java.util.ArrayList;
import java.util.Collection;
import java.util.Scanner;


/**
 * US010 - Register New Employee UI
 *
 * @author Guilherme Sousa <1211073@isep.ipp.pt>
 */

public class RegisterNewEmployeeUI implements Runnable {

    public RegisterNewEmployeeUI() {
    }

    private RegisterNewEmployeeController ctrl = new RegisterNewEmployeeController();


    public void run() {

        Scanner read = new Scanner(System.in);

        RegisterNewEmployeeDto dto = new RegisterNewEmployeeDto();
        System.out.printf("**You are now registering a new employee**%n");

        ArrayList<String> roles = ctrl.getRolesList();
        //roles().add(Constants.ROLE_NURSE); roles().add(Constants.ROLE_RECEPTIONIST); roles().add(Constants.ROLE_CENTRE_COORDINATOR);

        int index = Utils.showAndSelectIndex(roles, "**Select one of the options**");
        String selectedRole = roles.get(index);

        String givenName = Utils.readLineFromConsole("Insert Name: ");
        String givenAddress = Utils.readLineFromConsole("Insert Address: ");
        String givenPhoneNumber = Utils.readLineFromConsole("Insert Phone Number: ");
        String givenEmail = Utils.readLineFromConsole("Insert Email: ");
        String givenCitizenCardNumber = Utils.readLineFromConsole("Insert Citizen Card Number: ");
    }
}
